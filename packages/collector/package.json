{
	"name": "@cosquared/collector",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"license": "Apache-2.0",
	"description": "PUBLIC package (Apache-2.0 mirror allowlist): collectors, redaction, and scoring methodology. Depends on the public schema package ONLY — must never import any private workspace package.",
	"exports": {
		".": "./src/index.ts"
	},
	"scripts": {
		"typecheck": "tsc --noEmit"
	},
	"dependencies": {
		"@cosquared/schema": "workspace:^",
		"zod": "^4.0.0"
	},
	"devDependencies": {
		"@types/node": "^20.0.0",
		"typescript": "^5.0.0",
		"vitest": "^3.0.0"
	}
}
