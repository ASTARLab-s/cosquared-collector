/** Version of the strict external event envelope, independent of the API. */
export const EVAL_EVENT_SCHEMA_VERSION = "1.0.0";

/** Stable HTTP namespace for this contract generation. */
export const EVAL_API_VERSION = "v1";
